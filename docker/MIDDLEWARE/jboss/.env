# JBoss WildFly 설정 (공식 이미지)
WILDFLY_VERSION=latest-jdk11
JBOSS_HTTP_PORT=8080
JBOSS_ADMIN_PORT=9990
JBOSS_USER=admin
JBOSS_PASS=admin123

# Java 옵션
JAVA_OPTS=-Xms512m -Xmx2048m -XX:MetaspaceSize=256m -XX:MaxMetaspaceSize=512m

# 데이터베이스 설정
POSTGRES_VERSION=13
DB_NAME=jbossdb
DB_USER=jboss
DB_PASSWORD=jboss123
DB_PORT=5432
