server:
  sources:
    - path: "/folder"
      config:
        defaultEnabled: true 
        defaultUserScope: "/"
        createUserDir: false
auth:
  adminUsername: admin
  adminPassword: Admin2580!
