services:
  termix:
    image: ghcr.io/lukegus/termix:latest
    container_name: termix
    restart: unless-stopped
    ports:
      - "8880:8080"
    volumes:
      - /APP/termix-data:/app/data
    environment:
      PORT: "8080"
